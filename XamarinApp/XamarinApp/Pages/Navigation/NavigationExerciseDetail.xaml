<?xml version="1.0" encoding="utf-8" ?>
<ContentPage Title="{Binding Name}" 
             Padding="20"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ic="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions"
             x:Class="XamarinApp.Pages.Navigation.NavigationExerciseDetail">
    <StackLayout>
        <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="12" />
        <ic:CircleImage Source="{Binding ImageUrl}" HorizontalOptions="Start" WidthRequest="60" HeightRequest="60" Aspect="AspectFill" />
        <Label Text="{Binding Description}" FontSize="12" TextColor="Gray" />
    </StackLayout>
</ContentPage>